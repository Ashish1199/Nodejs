//Modules - Encapsulates Codes (only share minimum)
//Common JS, every file in node is a module.

const names = require('./4-names');
const sayHi = require('./5-utils');
const data =require("./6-alternatingflavour");
require("./7-mind-grenade")
// console.log(data);

// sayHi(names.Jhon);
// sayHi(names.Ashish);
